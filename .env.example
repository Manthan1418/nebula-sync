# Server port (Render sets this automatically)
PORT=3000

# CORS origin - use * for same-origin deployment (recommended for Render)
# For cross-origin: use comma-separated URLs like https://app1.com,https://app2.com
CLIENT_ORIGIN=*
